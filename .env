# ================================================================
# Sahakari SevaSetu — Frontend Environment Configuration
# ================================================================

# In local development, leave VITE_API_URL empty or set to /api so Vite's proxy forwards to http://localhost:5000
VITE_API_URL=/api

# If hosting frontend independently or tunneling backend via ngrok directly:
# VITE_API_URL=https://your-backend-tunnel.ngrok-free.app/api
